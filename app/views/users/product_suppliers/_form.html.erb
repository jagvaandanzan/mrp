
<div class="content-wrapper">

  <section class="content">
    <div class="box box-info">
      <%= form_with model: @product_supplier, url: @product_supplier.persisted? ? users_product_supplier_path : users_product_suppliers_path,
                    method: @product_supplier.persisted? ? :patch : :post, local: true, :html => {class: 'form-horizontal'} do |f| %>
        <%= form_body do %>
          <div class="form-group">
            <%= f.label :name, class: 'col-sm-3 control-label' %>
            <div class="col-sm-9 <%= get_error_class(@product_supplier, :name) %>">
              <%= field_errors(@product_supplier, :name) %>
              <%= f.text_field :name, class: 'form-control', placeholder: t('activerecord.attributes.product_supplier.name') %>
            </div>
          </div>
          <div class="form-group">
            <%= f.label :code, class: 'col-sm-3 control-label' %>
            <div class="col-sm-9 <%= get_error_class(@product_supplier, :code) %>">
              <%= field_errors(@product_supplier, :code) %>
              <%= f.text_field :code, class: 'form-control', placeholder: t('activerecord.attributes.product_supplier.code') %>
            </div>
          </div>
          <div class="form-group">
            <%= f.label :description, class: 'col-sm-3 control-label' %>
            <div class="col-sm-9 <%= get_error_class(@product_supplier, :description) %>">
              <%= field_errors(@product_supplier, :description) %>
              <%= f.text_area :description, class: 'form-control', placeholder: t('activerecord.attributes.product_supplier.description') %>
            </div>
          </div>

          <div class="action-buttons">
            <%= f.submit t('controls.button.save'), class: 'btn btn-warning' %>
            <%= link_to t('titles.back_to_list'), users_product_suppliers_path(page: cookies[:page_number]), {class: "btn blck-btn"} %>
          </div>
        <% end %>
      <% end %>
    </div>
  </section>
</div>